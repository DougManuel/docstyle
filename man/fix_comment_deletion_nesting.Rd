% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comments.R
\name{fix_comment_deletion_nesting}
\alias{fix_comment_deletion_nesting}
\title{Fix comment markers that should be nested inside track change deletions}
\usage{
fix_comment_deletion_nesting(docx_path, comments_json = NULL, verbose = FALSE)
}
\arguments{
\item{docx_path}{Path to .docx file to modify}

\item{comments_json}{Optional path to comments.json for metadata lookup.
If provided, only repositions comments that were originally inside deletions.}

\item{verbose}{If TRUE, prints progress messages}
}
\value{
Invisibly returns the number of comments repositioned
}
\description{
Post-processes a rendered DOCX to move comment markers that immediately follow
\verb{<w:del>} elements into the deletion, when the original source document had
the comment attached to deleted text.
}
\details{
This fixes a limitation of the Lua filter pipeline where \code{comment-inject.lua}
and \code{revisions-inject.lua} cannot nest OpenXML elements inside each other.

The function looks for this pattern in document.xml:

\if{html}{\out{<div class="sourceCode xml">}}\preformatted{<w:del ...>...</w:del>
<w:commentRangeStart w:id="X"/>
<w:commentRangeEnd w:id="X"/>
}\if{html}{\out{</div>}}

And transforms it to:

\if{html}{\out{<div class="sourceCode xml">}}\preformatted{<w:commentRangeStart w:id="X"/>
<w:del ...>...</w:del>
<w:commentRangeEnd w:id="X"/>
}\if{html}{\out{</div>}}

This positions the comment to span the deletion, which is semantically correct
when the comment was originally attached to the deleted text.
}
\examples{
\dontrun{
# After rendering with Quarto
fix_comment_deletion_nesting("output/document.docx")
}

}
\keyword{internal}
