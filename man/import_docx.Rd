% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{import_docx}
\alias{import_docx}
\title{Import a Word Document to Quarto}
\usage{
import_docx(
  docx_path,
  output_dir = NULL,
  qmd_name = NULL,
  extract_images = TRUE,
  image_dir = "images",
  config_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{docx_path}{Path to the input .docx file.}

\item{output_dir}{Directory for output files. Defaults to the same directory
as the input file.}

\item{qmd_name}{Name for the output .qmd file (without extension). Defaults
to the input filename.}

\item{extract_images}{Whether to extract embedded images (default: TRUE).}

\item{image_dir}{Directory for extracted images, relative to output_dir

(default: "images").}

\item{config_path}{Optional path to \verb{_quarto.yml}. If provided, the output
QMD will be configured to use docstyle rendering.}

\item{verbose}{If TRUE (default), prints progress messages.}
}
\value{
Invisibly returns a list with paths to all generated files:
\itemize{
\item \code{qmd_path}: Path to the generated .qmd file
\item \code{references_json}: Path to references.json (CSL-JSON for rendering)
\item \code{field_codes_json}: Path to field-codes.json (for Zotero round-trip)
}
}
\description{
The main entry point for converting Word documents to Quarto format. Extracts
content, preserves Zotero citations, and sets up the project for round-trip
editing.
}
\details{
\subsection{What \code{import_docx()} does}{
\enumerate{
\item Extracts Zotero citations and generates:
\itemize{
\item \code{references.json}: CSL-JSON for rendering citations
\item \code{field-codes.json}: Zotero metadata for round-trip field code injection
}
\item Converts Word content to Quarto markdown
\item Optionally extracts embedded images
\item Creates a ready-to-edit QMD with proper YAML header
}
}

\subsection{Citation handling}{

Unlike typical Quarto workflows that use \code{.bib} files, docstyle keeps
reference metadata in JSON format—similar to how Zotero manages citations
in Word. This approach:
\itemize{
\item Avoids conflicts when merging citation databases
\item Keeps metadata "behind the scenes" during editing
\item Enables seamless round-trip with \verb{quarto render}
}

To add new citations, use Zotero integration directly in the QMD.
}

\subsection{Round-trip workflow}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# 1. Collaborator sends edited Word document
result <- import_docx("collaborator-edits.docx")

# 2. Review and edit the QMD
# (use git diff to see what changed)

# 3. Render back to Word with the docstyle extension
# quarto render collaborator-edits.qmd
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Basic import
result <- import_docx("paper.docx")

# Import to a specific directory
result <- import_docx("paper.docx", output_dir = "project/")

# Import with docstyle configuration
result <- import_docx("paper.docx", config_path = "_quarto.yml")
}

}
\seealso{
\itemize{
\item \code{\link[=extract_citations]{extract_citations()}} for citation extraction only
\item \code{\link[=docx_to_qmd]{docx_to_qmd()}} for content conversion only
}
}
