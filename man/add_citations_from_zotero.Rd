% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_citations.R
\name{add_citations_from_zotero}
\alias{add_citations_from_zotero}
\title{Add citations to field-codes.json via local Zotero API}
\usage{
add_citations_from_zotero(
  citekeys,
  project_dir = ".",
  sidecar_dir = "_docstyle",
  write_bib = TRUE
)
}
\arguments{
\item{citekeys}{Character vector of Better BibTeX citation keys
(e.g. \code{c("tyas1998", "vangeli2011")}).}

\item{project_dir}{Path to the project root directory. Default: current
working directory.}

\item{sidecar_dir}{Name of the sidecar directory within \code{project_dir}.
Default: \code{"_docstyle"}.}

\item{write_bib}{If \code{TRUE} (default), calls \code{\link[=export_bibliography]{export_bibliography()}} after
adding entries so that \code{references.bib} is kept in sync for LaTeX/Typst
workflows. Set to \code{FALSE} to skip BibTeX export.}
}
\value{
Invisibly returns the updated field-codes list.
}
\description{
Searches the Zotero library for each citekey and appends the item metadata
to \verb{_docstyle/field-codes.json}. Does not require Word or a harvest
round-trip. After running, \verb{quarto render} injects live Zotero field codes
automatically via the \code{build_citation_instr()} fallback in
\code{inject_zotero_citations()}.
}
\details{
Requires Zotero to be running with the Better BibTeX extension installed.
Connectivity is checked via \code{\link[=is_zotero_running]{is_zotero_running()}} before any queries are
made.

Each citekey is looked up using the local Zotero HTTP API
(\verb{http://127.0.0.1:23119/api/}). The returned CSL-JSON \code{itemData} and
Zotero item URI are written into the \code{citations} catalog of
\code{field-codes.json}.

Existing entries are never overwritten. Citekeys not found in Zotero
produce a warning and are skipped.
}
\examples{
\dontrun{
# Add two citations to the sidecar, then render
add_citations_from_zotero(c("tyas1998", "vangeli2011"))

# Suppress BibTeX sync (e.g. docx-only workflow)
add_citations_from_zotero("tyas1998", write_bib = FALSE)
}

}
\seealso{
\code{\link[=export_bibliography]{export_bibliography()}} to export the full bibliography to BibTeX.
}
