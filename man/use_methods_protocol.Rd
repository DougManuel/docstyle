% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_methods_protocol.R
\name{use_methods_protocol}
\alias{use_methods_protocol}
\title{Scaffold a methods-protocol Quarto project}
\usage{
use_methods_protocol(
  path = ".",
  title = "Working title",
  framework = c("prisma-scr", "prisma-p"),
  overwrite = FALSE
)
}
\arguments{
\item{path}{Path to the directory in which to scaffold the project.
Will be created if it does not exist. Defaults to current directory.}

\item{title}{Working title for the protocol. Substituted into
\verb{_quarto.yml} (running head, footer) and \code{protocol.qmd} (title
field). Can be edited later. Must not be empty.}

\item{framework}{One of \code{"prisma-scr"} (scoping reviews; default) or
\code{"prisma-p"} (systematic reviews). Selects the section scaffold
in \code{protocol.qmd}.}

\item{overwrite}{Logical. If TRUE, overwrite existing template files
at the target path AND replace the bundled docstyle extension at
\verb{_extensions/docstyle/}. If FALSE (default), refuse to overwrite
any existing template file and abort. The \code{overwrite} flag is
passed through to \code{\link[=use_docstyle]{use_docstyle()}} for the extension copy.}
}
\value{
Invisibly, the absolute path to the scaffolded project.
}
\description{
Creates a working Quarto project pre-configured for both Word editing
(\code{docstyle-docx} with Zotero field codes) and medRxiv-ready preprint
output (\code{docstyle-typst} with \code{medrxiv: true}). Drops in a PRISMA-ScR
or PRISMA-P scaffolded \code{protocol.qmd} with section headings and inline
prompts, a \code{references.bib} skeleton, a \verb{supplements/} directory with
a README and starter stubs (\code{search-strategy.json},
\code{data-charting-fields.csv} or \code{data-extraction-fields.csv}), a
project README with a submission checklist, and the bundled docstyle
extension.
}
\details{
Removes the cold-start cost of starting a methods protocol — open the
rendered Word doc and start writing, or render the Typst PDF for
preprint review immediately.
}
\examples{
\dontrun{
# Scoping review protocol scaffold
use_methods_protocol(
  path = "popcorn-protocol",
  title = "POPCORN scoping review protocol",
  framework = "prisma-scr"
)

# Systematic review protocol scaffold
use_methods_protocol(
  path = "rct-review",
  title = "Effect of X on Y in Z populations",
  framework = "prisma-p"
)
}
}
