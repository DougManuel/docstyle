% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{find_project_root}
\alias{find_project_root}
\title{Find the docstyle project root directory}
\usage{
find_project_root(start = getwd(), max_levels = 8)
}
\arguments{
\item{start}{Directory to start searching from. Defaults to the current
working directory.}

\item{max_levels}{Maximum number of parent directories to check. Default 8.}
}
\value{
Absolute path to the project root directory.
}
\description{
Walks upward from \code{start} looking for the nearest ancestor that contains
\verb{_quarto.yml} with a \verb{docstyle:} section, a root \verb{_quarto.yml} (project
file), or a \code{.git} directory. Falls back to \code{QUARTO_PROJECT_DIR} if set,
then to \code{start} itself.
}
\details{
This is used by the pre- and post-render scripts to resolve sidecar paths
robustly when rendering from a subdirectory (e.g., \verb{docs/}) that has its
own \verb{_quarto.yml}.
}
