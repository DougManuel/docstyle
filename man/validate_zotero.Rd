% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_zotero.R
\name{validate_zotero}
\alias{validate_zotero}
\title{Validate Zotero Field Codes in Word Documents}
\usage{
validate_zotero(docx_path, verbose = TRUE)
}
\arguments{
\item{docx_path}{Path to the DOCX file to validate}

\item{verbose}{Logical. Print detailed validation output. Default TRUE.}
}
\value{
A list with validation results:
\describe{
\item{valid}{Logical. TRUE if all critical checks passed}
\item{summary}{List with counts: items, bibl, pref, field_codes}
\item{checks}{Named list of individual check results}
\item{issues}{List with errors (critical) and warnings (non-critical)}
\item{zotero_pref}{Parsed ZOTERO_PREF content, if found}
}
}
\description{
Comprehensive validation for Zotero field codes in DOCX files. Checks
field code structure, ZOTERO_PREF presence, citation JSON validity,
and bibliography structure.
}
\details{
The validator checks:
\itemize{
\item \strong{Field code structure}: Balanced begin/separate/end sequences
\item \strong{ZOTERO_PREF}: Document preferences field exists with valid JSON
\item \strong{ZOTERO_ITEM}: Each citation has valid CSL_CITATION JSON
\item \strong{ZOTERO_BIBL}: Bibliography field has valid structure
\item \strong{No orphan references}: All citation IDs properly closed
}
}
\examples{
\dontrun{
# Validate a document
result <- validate_zotero("document.docx")

if (!result$valid) {
  cat("Zotero validation failed:\n")
  for (err in result$issues$errors) {
    cat("  ERROR:", err, "\n")
  }
}

# Check if ZOTERO_PREF is missing
if (!result$checks$has_zotero_pref) {
  cat("Missing ZOTERO_PREF - Zotero may be unstable\n")
}
}

}
