% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_to_qmd.R
\name{docx_to_qmd}
\alias{docx_to_qmd}
\title{Extract Word Document to Quarto Markdown}
\usage{
docx_to_qmd(
  docx_path,
  output_path = NULL,
  sidecar_dir = NULL,
  style = NULL,
  extract_images = TRUE,
  image_dir = "images",
  validate_bib = NULL,
  preserve_header = TRUE,
  project = c("none", "init", "update", "overwrite"),
  preset = "default",
  version_history = TRUE,
  extract_styles = FALSE,
  validate = FALSE
)
}
\arguments{
\item{docx_path}{Path to the input .docx file}

\item{output_path}{Path for the output .qmd file (default: same name with .qmd extension)}

\item{sidecar_dir}{Directory for sidecar files (field-codes.json, references.json).
Default: "_docstyle" subdirectory in the output directory.}

\item{style}{Style name to use (e.g., "popcorn"). If provided, copies the reference
document to the output directory and adds it to the YAML header.}

\item{extract_images}{Whether to extract embedded images (default: TRUE)}

\item{image_dir}{Directory for extracted images (default: "images")}

\item{validate_bib}{Optional path to a canonical BibTeX file to validate extracted citations against.}

\item{preserve_header}{If TRUE (default) and the output file already exists, preserves
the existing YAML front matter. The old file is renamed to \verb{*_old.qmd} as a backup.
This allows re-harvesting from Word while keeping manually curated metadata (authors,
affiliations, version info, etc.).}

\item{project}{Character. Controls project infrastructure setup:
\itemize{
\item \code{"none"} (default): Just QMD + sidecar files, no project setup
\item \code{"init"}: Create \verb{_quarto.yml}, CSS, extension if missing; skip if exists
\item \code{"update"}: Create if missing; merge new settings/styles if exists (Stage 3)
\item \code{"overwrite"}: Always replace project config (destructive)
}}

\item{preset}{Character. Style preset to use when \code{project != "none"}. Can be:
\itemize{
\item A preset name: \code{"default"}, \code{"formal"}, or \code{"academic"}
\item A path to a custom preset folder containing \verb{_quarto.yml} and \code{styles.css}
}}

\item{version_history}{Logical. If TRUE (default) and \code{project != "none"}, inserts
a \verb{::: version-history :::} placeholder and adds initial version entry to YAML.}

\item{extract_styles}{Logical. If TRUE and \code{project != "none"}, extracts font,
colour, and page layout styles from the Word document and writes them to CSS.
Default is FALSE. When TRUE, styles are appended to the project's CSS file.}

\item{validate}{Logical. If TRUE, runs \code{\link{validate_harvest}} after
writing the QMD to check fidelity against the source. Also triggered by
setting environment variable \code{DOCSTYLE_VALIDATE_HARVEST=1}.}
}
\value{
Invisibly returns the path to the created .qmd file
}
\description{
Converts a .docx file to .qmd format, preserving structure and styles.
Automatically extracts Zotero citations and converts them to Quarto format.
}
