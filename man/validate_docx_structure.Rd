% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_structure.R
\name{validate_docx_structure}
\alias{validate_docx_structure}
\title{Validate DOCX structural integrity}
\usage{
validate_docx_structure(
  docx_path,
  checks = c("xml", "whitespace", "deletions", "insertions", "ids", "rels",
    "content_types", "rsids"),
  verbose = TRUE
)
}
\arguments{
\item{docx_path}{Path to the DOCX file to validate}

\item{checks}{Character vector of checks to run. Default runs all checks
except \code{"xsd"} and \code{"ooxml"}, which require external tools (\code{xmllint},
\code{npx}). Available checks: "xml", "whitespace", "deletions", "insertions",
"ids", "rels", "content_types", "rsids", "xsd", "ooxml"}

\item{verbose}{Logical. Print detailed validation output. Default TRUE.}
}
\value{
A list with:
\describe{
\item{valid}{Logical. TRUE if all checks passed}
\item{checks}{Named list of individual check results}
\item{errors}{Character vector of error messages}
\item{warnings}{Character vector of warning messages}
}
}
\description{
Deep validation of OOXML structure to catch potential corruption before
opening in Word. Checks XML well-formedness, ID uniqueness, relationship
integrity, and content type declarations.
}
\details{
This function validates the internal structure of a DOCX file to catch
issues that would cause Word to report "unreadable content" errors.
\subsection{Checks performed}{
\itemize{
\item \strong{xml}: All XML files are well-formed
\item \strong{whitespace}: w:t elements with leading/trailing whitespace have
xml:space="preserve"
\item \strong{deletions}: \code{w:t} elements inside \code{w:del} are flagged (deleted text must use \code{w:delText})
\item \strong{insertions}: w:delText not inside w:ins without w:del wrapper
\item \strong{ids}: Comment, bookmark, and range IDs are unique within their scope
\item \strong{rels}: All relationship references are valid and all files are referenced
\item \strong{content_types}: All content files are declared in [Content_Types].xml
\item \strong{rsids}: All RSID attribute values are 8-digit hexadecimal (0-9, A-F,
case-insensitive). Invalid RSIDs can cause Word to report "unreadable
content" errors. Errors are reported as warnings only (Word tolerates some
RSID deviations). Only \code{word/document.xml} is checked; RSIDs in
\code{settings.xml}, \code{footnotes.xml}, and \code{comments.xml} are not validated.
\item \strong{xsd}: XML files validate against OOXML schemas (requires xmllint).
Note: XSD errors are reported as warnings only, not failures, because
Pandoc-generated documents often have minor schema deviations that
Word tolerates.
\item \strong{ooxml}: Validate against Microsoft Open XML SDK schemas via
\verb{@xarsh/ooxml-validator} (requires npx). Not in the default check set.
Errors are reported as warnings only. See \code{\link[=validate_ooxml_schema]{validate_ooxml_schema()}}.
}
}
}
\examples{
\dontrun{
# Full validation
result <- validate_docx_structure("output/document.docx")

# Quick validation (skip XSD)
result <- validate_docx_structure("output/document.docx",
  checks = c("xml", "whitespace", "ids", "rels"))

# Programmatic use
result <- validate_docx_structure("output/document.docx", verbose = FALSE)
if (!result$valid) {
  stop("Structural validation failed: ", paste(result$errors, collapse = "; "))
}
}

}
\seealso{
\code{\link[=validate_docx]{validate_docx()}} for property-based validation,
\code{\link[=validate_comments]{validate_comments()}} for comment-specific validation
}
