% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_docstyle.R
\name{check_project}
\alias{check_project}
\title{Check Project Health}
\usage{
check_project(project_dir = ".", qmd_files = NULL, verbose = TRUE)
}
\arguments{
\item{project_dir}{Path to the Quarto project directory. Defaults to
current working directory.}

\item{qmd_files}{Character vector of QMD file paths to check. If NULL
(default), auto-detects QMD files in the project directory.}

\item{verbose}{Logical. If TRUE (default), prints diagnostic messages.}
}
\value{
A list with:
\itemize{
\item \code{valid}: TRUE if no errors found
\item \code{checks}: named logical list of individual check results
\item \code{issues}: list with \code{errors} and \code{warnings} character vectors
}
}
\description{
Validates that a docstyle project is correctly configured. Checks extension
installation and completeness, Quarto format configuration, QMD header
pitfalls, sidecar directory and JSON validity, citation coverage and
bibliography placeholder, and version consistency.
}
\examples{
\dontrun{
result <- check_project()
if (!result$valid) {
  cat("Errors:\n")
  for (err in result$issues$errors) cat("  -", err, "\n")
}
}

}
