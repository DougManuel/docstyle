% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/css_injection.R
\name{preview_css_mapping}
\alias{preview_css_mapping}
\title{Preview how CSS selectors map to Word styles}
\usage{
preview_css_mapping(css_path = NULL, project_dir = ".")
}
\arguments{
\item{css_path}{Path to a CSS file, or NULL to use the extension's
\code{default.css}. Accepts a character vector of multiple paths (merged in
order, later files win).}

\item{project_dir}{Path to the project directory, used to resolve relative
CSS paths. Defaults to the current working directory.}
}
\value{
Invisibly returns a list of style mappings. Each element has:
\itemize{
\item \code{selector}: the CSS selector
\item \code{word_style}: the mapped Word style name (or \code{"(unmapped)"})
\item \code{word_style_id}: the Word style ID
\item \code{properties}: named character vector of CSS properties
\item \code{pPr}: translated paragraph properties (list)
\item \code{rPr}: translated run properties (list)
Prints a formatted summary to the console.
}
}
\description{
Dry-run the CSS-to-Word style translation pipeline without rendering a
document. Reads a CSS file (or uses the docstyle default), maps each
selector to its Word style equivalent, and summarizes the translated
properties in human-readable form.
}
\details{
Useful for debugging CSS configurations and understanding how CSS properties
will appear in the final Word document.
}
\examples{
\dontrun{
# Preview default CSS
preview_css_mapping()

# Preview a project CSS file
preview_css_mapping("styles.css")

# Preview multiple CSS files (merged)
preview_css_mapping(c("base.css", "overrides.css"))
}

}
