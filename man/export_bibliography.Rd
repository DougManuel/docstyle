% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_citations.R
\name{export_bibliography}
\alias{export_bibliography}
\title{Export bibliography as BibTeX from harvested sidecar data}
\usage{
export_bibliography(sidecar_dir, output = NULL, verbose = TRUE)
}
\arguments{
\item{sidecar_dir}{Path to the \verb{_docstyle} sidecar directory containing
\code{field-codes.json} and/or \code{references.json}.}

\item{output}{Path for the output \code{.bib} file. Default writes
\code{references.bib} in the sidecar directory.}

\item{verbose}{Print progress messages. Default TRUE.}
}
\value{
Invisibly returns the output path.
}
\description{
Reads citation data from the sidecar directory (\code{field-codes.json} or
\code{references.json}) and writes a \code{.bib} file with BibTeX entries. Citation
keys match those used in the QMD (\verb{[@key]} syntax).
}
\details{
The function prefers \code{field-codes.json} as the source because it contains
citation keys that match the QMD. Falls back to \code{references.json} if
field-codes.json is absent. Uses the internal \code{csl_to_bibtex_with_key()}
converter for each item.
}
\examples{
\dontrun{
# After harvest, export bibliography for preprint rendering
export_bibliography("_docstyle")

# Custom output path
export_bibliography("_docstyle", output = "references.bib")
}

}
