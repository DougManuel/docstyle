% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_arxiv.R
\name{package_arxiv}
\alias{package_arxiv}
\title{Package a rendered LaTeX document for arXiv submission}
\usage{
package_arxiv(
  tex_path,
  output_path = NULL,
  style_files = NULL,
  bib_path = NULL,
  extra_files = NULL,
  flatten = TRUE,
  archive_format = c("tar.gz", "zip"),
  verbose = TRUE
)
}
\arguments{
\item{tex_path}{Path to the rendered \code{.tex} file.}

\item{output_path}{Optional output archive path. Defaults to
\verb{<tex_stem>-arxiv.<ext>} in the same directory as \code{tex_path}, where
\verb{<ext>} matches \code{archive_format} (\code{.tar.gz} or \code{.zip}).}

\item{style_files}{Optional character vector of \code{.sty}/\code{.cls} paths to
bundle. If NULL, auto-discovers files in the \code{.tex} directory whose
stem is \verb{\\usepackage\{...\}}'d or \verb{\\documentclass\{...\}}'d by the tex.}

\item{bib_path}{Optional path to a \code{.bib} file. If NULL and the \code{.tex}
contains \verb{\\bibliography\{...\}}, auto-detects the first entry. A \code{.bbl}
sibling of the \code{.tex} (biber/bibtex output) is auto-included
independently. For citeproc-inlined bibliographies, no file is needed.}

\item{extra_files}{Optional character vector of additional files to
include in the archive (e.g. a README, cover letter).}

\item{flatten}{Logical. If TRUE (default), subdirectory references like
\verb{\\includegraphics\{images/fig1.png\}} are flattened to
\verb{\\includegraphics\{fig1.png\}} and figures copied to the archive root.
arXiv's submission guidance recommends a flat directory layout;
flattening is the safest default.}

\item{archive_format}{One of \code{"tar.gz"} (arXiv's documented preference) or
\code{"zip"} (also accepted).}

\item{verbose}{Logical. Print progress messages.}
}
\value{
Invisibly, a list with:
\itemize{
\item \code{archive_path}: path to the created archive
\item \code{manifest}: character vector of files included
\item \code{rewrites}: named character vector of \code{old_path = new_path} for
figures that were flattened
\item \code{warnings}: character vector of non-fatal issues
}
}
\description{
Takes a rendered \code{.tex} file (typically from \verb{quarto render} with
\code{keep-tex: true}) and produces a submission-ready archive:
\itemize{
\item copies figures referenced by \verb{\\includegraphics\{...\}} into the staging
directory, optionally flattening subdirectory paths;
\item copies \code{.sty}/\code{.cls} files from the \code{.tex} directory that are actually
\verb{\\usepackage}'d or \verb{\\documentclass}'d by the document;
\item rewrites \verb{\\includegraphics} paths to match the flattened layout;
\item runs a pre-flight check (every figure resolves, no parent-directory
references, no basename collisions);
\item assembles a \code{.tar.gz} or \code{.zip} archive.
}
}
\details{
The function is format-agnostic -- it takes a \code{.tex} path as input, so any
LaTeX-producing Quarto format works (e.g. \code{arxiv-pdf},
\code{mikemahoney218/quarto-arxiv}, or a future \code{docstyle-arxiv}).
}
