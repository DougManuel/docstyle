% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_extension_drift.R
\name{check_extension_drift}
\alias{check_extension_drift}
\title{Check vendored docstyle extension version against installed package}
\usage{
check_extension_drift(
  project_dir = ".",
  installed_version = NULL,
  vendored_version = NULL
)
}
\arguments{
\item{project_dir}{Path to the project root (containing \verb{_quarto.yml}
and \verb{_extensions/docstyle/}). Defaults to the current working
directory.}

\item{installed_version}{Optional character string for the installed
docstyle package version. If NULL, reads from
\code{utils::packageVersion("docstyle")}. Useful for testing.}

\item{vendored_version}{Optional character string for the vendored
extension version. If NULL, reads from
\verb{<project_dir>/_extensions/docstyle/_extension.yml}. Useful for
testing.}
}
\value{
A list with:
\itemize{
\item \code{status}: one of \code{"match"} (versions equal), \code{"drift"} (different),
\code{"no-extension"} (vendored extension or its manifest is missing),
\code{"no-package"} (docstyle R package not installed/findable).
\item \code{installed}: character string of the installed package version,
or NULL if not findable.
\item \code{vendored}: character string of the vendored extension version,
or NULL if not findable.
\item \code{message}: optional user-facing message (NULL when no action
needed). Tagged with the \verb{[check-extension]} prefix.
}
}
\description{
Compares the version declared in the project's vendored
\verb{_extensions/docstyle/_extension.yml} with the installed docstyle R
package version. Returns a structured result so callers can either
surface a render-time warning or take action.
}
\details{
This is the post-render-validators pattern (#145) applied at the
pre-render layer: catch a class of silent failure (vendored
extension drifts, the project no longer matches the package's
behaviour) at the moment the author is most likely to notice and
act on it — render time.
}
\examples{
\dontrun{
result <- check_extension_drift(".")
if (!is.null(result$message)) message(result$message)
}
}
