% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_docstyle.R
\name{use_docstyle}
\alias{use_docstyle}
\title{Install docstyle Quarto Extension}
\usage{
use_docstyle(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Path to the Quarto project directory. Defaults to current
working directory.}

\item{overwrite}{Logical. If TRUE, overwrites existing extension files.}
}
\value{
Invisibly returns the path to the installed extension directory.
}
\description{
Installs the docstyle Quarto extension into a project directory.
This copies the extension files from the R package to the project's
\verb{_extensions/docstyle/} directory.
}
\details{
This function provides an alternative to \verb{quarto add dmanuel/docstyle} that
works offline and ensures version consistency with the R package.

The extension includes:
\itemize{
\item Lua filters for character styles, tables, TOC, version history, etc
\item R scripts for reference document generation and post-render processing
\item Extension configuration (\verb{_extension.yml})
}

After installation, configure your \verb{_quarto.yml} to use the extension:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{format:
  docstyle-docx:
    docstyle:
      css: styles.css
      # ... other options
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
# Install in current project
use_docstyle()

# Install in a specific project
use_docstyle("path/to/my-quarto-project")

# Force update existing installation
use_docstyle(overwrite = TRUE)
}

}
