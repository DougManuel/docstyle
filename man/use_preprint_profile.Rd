% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_docstyle.R
\name{use_preprint_profile}
\alias{use_preprint_profile}
\title{Set Up Preprint Profile}
\usage{
use_preprint_profile(path = ".", overwrite = FALSE, theme = "man")
}
\arguments{
\item{path}{Path to the Quarto project directory. Defaults to current
working directory.}

\item{overwrite}{Logical. If TRUE, overwrite an existing
\verb{_quarto-preprint.yml}. Default FALSE. No backup is created before
overwriting.}

\item{theme}{Character. preprint-typst theme. One of \code{"jou"} (journal,
two-column), \code{"man"} (manuscript, one-column), \code{"stu"} (student,
one-column). Default \code{"man"}.}
}
\value{
Invisibly returns the path to the written file.
}
\description{
Writes \verb{_quarto-preprint.yml} into a docstyle project, enabling the same
QMD source to render to preprint-typst (PDF) without modification. The
profile uses \code{strip-docstyle.lua} (bundled in the extension) to handle
docstyle-specific constructs -- section wrappers, author plates, version
history, bibliography placement -- that don't apply to typst output.
}
\details{
To render with the profile:

\if{html}{\out{<div class="sourceCode">}}\preformatted{quarto render document.qmd --profile preprint
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
use_preprint_profile()
use_preprint_profile(theme = "jou")
}

}
\seealso{
\code{\link[=use_docstyle]{use_docstyle()}} to install the extension
}
