% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/css_reader.R
\name{parse_page_rules}
\alias{parse_page_rules}
\title{Parse @page Rules from CSS}
\usage{
parse_page_rules(text)
}
\arguments{
\item{text}{Raw CSS string.}
}
\value{
List with page configuration (size, margins, orientation, line-numbers).
Named page rules are stored in \verb{$named} as a list keyed by name.
}
\description{
Extracts \verb{@page} rules from CSS content and converts them to a page
configuration structure compatible with docstyle's page settings.
}
\details{
Supports standard CSS Paged Media properties:
\itemize{
\item \code{size}: letter | a4 | legal, optionally with orientation (e.g., "letter landscape")
\item \code{margin}: shorthand or individual margin-top, margin-right, etc.
}

Also supports docstyle extensions via CSS custom properties:
\itemize{
\item \code{--docstyle-line-numbers}: "every 1" | "every 5" | "none"
\item \code{--docstyle-line-numbers-restart}: "page" | "section" | "continuous"
\item \code{--docstyle-line-numbers-distance}: CSS length (e.g., "0.25in")
\item \code{--docstyle-suppress-top-spacing}: "true" | "false" (suppress before-spacing
on the first content paragraph after a section break)
}

Named page rules (e.g., \verb{@page landscape \{ ... \}}) are stored in \verb{$named}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{page_config$named$landscape  # Properties for @page landscape
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
css <- '@page { size: letter; margin: 1in; }
        @page landscape { size: letter landscape; margin: 0.5in; }'
page_config <- parse_page_rules(css)
# Returns: list(
#   size = "letter",
#   margins = list(top = "1in", ...),
#   named = list(landscape = list(size = "letter", orientation = "landscape", ...))
# )
}
}
\keyword{internal}
