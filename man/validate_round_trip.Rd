% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_round_trip.R
\name{validate_round_trip}
\alias{validate_round_trip}
\title{Validate full round-trip: harvest, render, and output validation}
\usage{
validate_round_trip(
  docx_path,
  qmd_path,
  sidecar_dir = NULL,
  output_dir = NULL,
  render = TRUE,
  structure_checks = c("xml", "whitespace", "deletions", "insertions", "ids", "rels",
    "content_types"),
  verbose = TRUE
)
}
\arguments{
\item{docx_path}{Path to the source .docx file}

\item{qmd_path}{Path to the harvested .qmd file}

\item{sidecar_dir}{Path to the _docstyle/ sidecar directory. If NULL,
auto-detected from the QMD directory.}

\item{output_dir}{Directory for rendered output. Default: "output/" relative
to the QMD file.}

\item{render}{Logical. If TRUE, runs \verb{quarto render}. If FALSE, looks for
an existing output docx.}

\item{structure_checks}{Character vector of checks for
\code{\link[=validate_docx_structure]{validate_docx_structure()}}. Default runs all standard checks.}

\item{verbose}{Logical. Print progress output. Default TRUE.}
}
\value{
A \code{docstyle_validation} object (list) with:
\describe{
\item{valid}{Logical. TRUE if all stages passed}
\item{stages}{Named list of per-stage results (harvest, render,
structure, comments)}
\item{issues}{List with aggregated errors and warnings}
}
}
\description{
Orchestrates the complete validation sequence: checks that the harvest
faithfully captured the source document, renders the QMD back to Word,
and validates the output DOCX structure. Establishes a clean baseline
so that post-edit failures are attributable to edits, not the pipeline.
}
\examples{
\dontrun{
# Full round-trip validation
result <- validate_round_trip("source/document.docx", "document.qmd")

# Skip render, validate existing output
result <- validate_round_trip("source/document.docx", "document.qmd",
  render = FALSE)

# Print detailed results
print(result, detail = "full")

# Write report to file
report(result, file = "validation-report.md")
}

}
\seealso{
\code{\link[=validate_harvest]{validate_harvest()}} for harvest-only validation,
\code{\link[=validate_docx_structure]{validate_docx_structure()}} for output structural validation,
\code{\link[=validate_comments]{validate_comments()}} for comment round-trip validation
}
