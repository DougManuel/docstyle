% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_zotero.R
\name{inject_zotero_pref}
\alias{inject_zotero_pref}
\title{Inject ZOTERO_PREF into a Word Document}
\usage{
inject_zotero_pref(
  docx_path,
  style_id = "http://www.zotero.org/styles/vancouver",
  output_path = docx_path,
  replace_existing = FALSE,
  field_type = "Fields",
  journal_abbreviations = TRUE,
  store_references = TRUE
)
}
\arguments{
\item{docx_path}{Path to the DOCX file}

\item{style_id}{CSL style ID (default: Vancouver/numbered)}

\item{output_path}{Output path (default: overwrite input)}

\item{replace_existing}{If TRUE, replace existing ZOTERO_PREF. If FALSE,
only add if missing.}

\item{field_type}{"Fields" or "Bookmarks" (default: "Fields"). Passed to
\code{build_zotero_pref_xml()}.}

\item{journal_abbreviations}{Passed to \code{build_zotero_pref_xml()}
(default: TRUE).}

\item{store_references}{Passed to \code{build_zotero_pref_xml()}
(default: TRUE).}
}
\value{
The output path, invisibly
}
\description{
Adds or replaces the ZOTERO_PREF field in a DOCX document. This can
restore Zotero functionality when the preferences field is missing.
}
\examples{
\dontrun{
# Add missing ZOTERO_PREF with Vancouver style
inject_zotero_pref("document.docx")

# Use APA style
inject_zotero_pref("document.docx",
  style_id = "http://www.zotero.org/styles/apa")
}

}
\keyword{internal}
