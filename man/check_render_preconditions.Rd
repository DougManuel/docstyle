% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preflight.R
\name{check_render_preconditions}
\alias{check_render_preconditions}
\title{Check render preconditions (P0 footguns)}
\usage{
check_render_preconditions(project_dir = ".", config = NULL, qmd_files = NULL)
}
\arguments{
\item{project_dir}{Project root containing \verb{_quarto.yml}.}

\item{config}{Optional pre-parsed \verb{_quarto.yml} as a list. Read from
\code{project_dir} when \code{NULL}.}

\item{qmd_files}{Optional character vector of QMD paths to check.
Defaults to non-recursive \code{.qmd} files in \code{project_dir}.}
}
\value{
A list with \code{ok} (logical) and \code{errors} (character vector of
actionable messages; empty when \code{ok}).
}
\description{
Runs the small set of configuration checks that, when violated, produce
silently broken \code{docstyle-docx} output: \verb{bibliography:}, \verb{csl:},
\verb{reference-doc:}, or a \code{format: docx} override in a QMD header, and a
plain \code{format: docx} in \verb{_quarto.yml}. Called automatically by the
pre-render hook when the project renders \code{docstyle-docx}; disable with
\code{docstyle: preflight: false} in \verb{_quarto.yml}.
}
\details{
Unlike \code{\link[=check_project]{check_project()}}, which is a full project audit, this function
is deliberately minimal so it can run on every render without noise.
}
