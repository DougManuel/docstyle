% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_docstyle.R
\name{init}
\alias{init}
\title{Initialize a Quarto Project with docstyle}
\usage{
init(path = ".", preset = "default", overwrite = FALSE)
}
\arguments{
\item{path}{Path to the Quarto project directory. Defaults to current
working directory.}

\item{preset}{Character. Style preset to use. Can be either:
\itemize{
\item A preset name: \code{"default"}, \code{"formal"}, or \code{"academic"} (looks in
package's \verb{inst/presets/} directory)
\item A path to a custom preset folder containing \verb{_quarto.yml} and
\code{styles.css}
}}

\item{overwrite}{Logical. If TRUE, overwrites existing extension and
configuration. Default is FALSE.}
}
\value{
Invisibly returns a list with paths to created/modified files.
}
\description{
One-command setup that installs the docstyle extension and configures
your Quarto project. This is the recommended way to get started with
docstyle.
}
\details{
This function performs the following setup steps:
\enumerate{
\item \strong{Resolves preset}: Validates the preset folder contains \verb{_quarto.yml}
and \code{styles.css}
\item \strong{Installs extension}: Copies extension files to \verb{_extensions/docstyle/}
\item \strong{Applies preset}: Copies or merges \verb{_quarto.yml}, \code{styles.css}, and
any extra files from the preset
\item \strong{Installs bootstrap scripts}: Copies pre-render and post-render
scripts to \verb{_docstyle/}
}

After running \code{init()}, you can immediately render your document:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# In R
quarto::quarto_render("document.qmd")

# Or from terminal
# quarto render document.qmd
}\if{html}{\out{</div>}}
}
\section{Presets}{


Built-in presets:
\itemize{
\item \strong{default}: Neutral starting point (Arial 11pt, single-spaced)
\item \strong{formal}: Professional documents like reports and charters
(Calibri, coloured headings, TOC enabled)
\item \strong{academic}: Journal submission format (Times New Roman 12pt,
double-spaced, paragraph indents)
}

Custom presets can be stored anywhere and referenced by path:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{init(preset = "~/my-presets/cihr")
}\if{html}{\out{</div>}}

A preset folder must contain:
\itemize{
\item \verb{_quarto.yml}: Project configuration (merged with existing)
\item \code{styles.css}: Stylesheet (copied to project)
}
}

\examples{
\dontrun{
# Initialize with default preset
init()

# Initialize with formal preset (for reports)
init(preset = "formal")

# Initialize with custom preset from path
init(preset = "~/github/my-presets/cihr")

# Initialize a specific project directory
init("path/to/my-project", preset = "academic")
}

}
\seealso{
\code{\link[=use_docstyle]{use_docstyle()}} for extension-only installation without
configuration.
}
