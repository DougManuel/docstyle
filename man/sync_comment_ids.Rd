% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_comments.R
\name{sync_comment_ids}
\alias{sync_comment_ids}
\title{Synchronize comment IDs from source DOCX to QMD}
\usage{
sync_comment_ids(
  qmd_path,
  source_docx,
  comments_json = NULL,
  backup = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{qmd_path}{Path to the .qmd file to update}

\item{source_docx}{Path to the source .docx file with current comment IDs}

\item{comments_json}{Path to comments.json (default: auto-detect from _docstyle/)}

\item{backup}{If TRUE (default), creates a backup of the QMD before modifying}

\item{dry_run}{If TRUE, report what would change without modifying files}
}
\value{
A list with:
\describe{
\item{success}{Logical. TRUE if sync completed successfully}
\item{changes}{Data frame of ID mappings (old_id, new_id, matched_by)}
\item{unmapped_qmd}{IDs in QMD that couldn't be mapped to source DOCX}
\item{unmapped_docx}{IDs in source DOCX not referenced in QMD}
\item{message}{Human-readable summary}
}
}
\description{
Updates comment markers in a QMD file to match the IDs in a source DOCX.
This is needed when the source DOCX has been edited in Word and Word has
renumbered the comment IDs.
}
\details{
The function uses content-based matching to find corresponding comments:
\enumerate{
\item Extracts comments from source DOCX with their new IDs
\item Reads existing comments.json for old content
\item Matches comments by content similarity
\item Updates QMD markers with new IDs
\item Regenerates comments.json with new IDs
}
}
\examples{
\dontrun{
# Sync after Word editing
result <- sync_comment_ids(
  qmd_path = "document.qmd",
  source_docx = "source/document.docx"
)

# Preview changes without modifying
result <- sync_comment_ids(
  qmd_path = "document.qmd",
  source_docx = "source/document.docx",
  dry_run = TRUE
)
print(result$changes)
}

}
\keyword{internal}
